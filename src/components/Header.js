import React from 'react'
import { createComponent } from '@lit-labs/react'
import { Header as EfHeader } from '@refinitiv-ui/elements/header'

const Header = createComponent(React, 'ef-header', EfHeader, {})

export default Header
