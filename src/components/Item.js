import React from 'react'
import { createComponent } from '@lit-labs/react'
import { Item as EfItem } from '@refinitiv-ui/elements/item'

const Item = createComponent(React, 'ef-item', EfItem)

export default Item
